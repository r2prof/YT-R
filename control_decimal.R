# How to control decimal places in R?------

# Basic Syntax of round()
rounded_value <- round(original_value, digits = decimal_places)

# Example
original_value <- 3.141592653589793

rounded_value <- round(original_value, digits = 2)

# Output
rounded_value